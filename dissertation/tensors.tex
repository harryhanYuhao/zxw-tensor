\chapter{Tensors} \label{ch:tensors}

\section{Tensors and Basic Operations}

We present the following straightforward definition of tensors, which is a rephrasal of Penrose's original definition \cite{penrose1971applications}.

\begin{definition}\label{def:tensor}
Given $n\in \N$, let $[n]$ denote the set $\{0, 1, \cdots, n-1\}$. 
A tensor of rank $k \in \N$ is a function $T : [n_0] \times [n_1] \times \cdots [n_{k-1}] \rightarrow \C$.
\end{definition}

By covention, we shall write the value $T(i_0, i_1, \cdots, i_{k-1})$ as $T_{i_0 i_1 \cdots i_{k-1}}$, where each $i$ is called an index.
Of course, the output of $T$ can be $\R$ or any other ring. 
We ordain it to be $\C$ in this dissertation.
$n_j$ is called the dimension of the $j$-th index, which will always be clear from the context.

Following the convention of Einstein summation, a tensor $T$ is denoted by a capital letter with its indices as free variables shown in the subscript.
The class of all tensors is denoted by the caligraphy letter $\T_{ijk \cdots}$.

Tensor is, plainly and unonstensibly, an arrangement of complex numbers with some orderings by the indices.
For example, a rank-one tensor is a vector entries, rank-two tensor is a matrix, and a rank-three tensor is an arrangement of matrices.

The following equation denotes a vector and a matrix, while figure \ref{fig:rank3-stack} depicts the rank-three tensor as a stack of $n$ such matrix slices.
\begin{equation*}
	T_i =
	\begin{pmatrix}
		T_0 \\ T_1 \\ \vdots \\ T_{n-1}
	\end{pmatrix},
	\qquad
	T_{ij} =
	\begin{pmatrix}
		T_{00} & T_{01} & \cdots & T_{0,n-1} \\
		T_{10} & T_{11} & \cdots & T_{1,n-1} \\
		\vdots & \vdots & \ddots & \vdots \\
		T_{n-1,0} & T_{n-1,1} & \cdots & T_{n-1,n-1}
	\end{pmatrix}.
\end{equation*}

\begin{figure}[ht]
\centering
\input{figures/rank3-stack.tikz}
\caption{A rank-three tensor $T_{ijk}$ regarded as a stack of $n$ matrices.}
\label{fig:rank3-stack}
\end{figure}



There are at least seven natural operations for tensors.
\begin{observation}
	\begin{itemize} 
		\item \textbf{Addition} $\T + \T \rightarrow \T$. Let $T_{ijk \cdots}$ and $S_{ijk \cdots}$ be two tensors of the same rank. Then their sum is defined by $(T + S)_{ijk \cdots} = T_{ijk \cdots} + S_{ijk \cdots}$.
		\item \textbf{Scalar multiplication} Let $T_{ijk \cdots}$ be a tensor and $c \in \C$. Define $(cT)_{ijk \cdots} =c (T_{ijk \cdots}) $
		\item \textbf{Permutation.} Let $T_{ijk\cdots}$ be a tensor of rank $m$. For any permutation $\sigma \in S_m$, the permuted tensor is defined by $(\sigma \cdot T)_{i_{\sigma(1)} i_{\sigma(2)} \cdots i_{\sigma(k)}} = T_{i_1 i_2 \cdots i_k}$.
		\item \textbf{Flatten} $\T \rightarrow \T$. Let $T_{ijk \cdots}$ be a tensor. 
		Any of the two indices of $T$ can be flattened into one to create a new tensor.
		Say we would like to flatten the indices $i$ and $j$, of dimension $n_1, n_2$. Let $\alpha$ be an index of dimension $n_1n_2$, and we define the new tensor $S_{ak \cdots}$ as $S_{(n_2 i + j)k \cdots} = T_{ijk\cdots}$.
		\item \textbf{Unflatten}, which is the operation exactly opposite of flatten.
		\item \textbf{Outer product} $\T \otimes \T \rightarrow \T$ Let $T_{ijk \cdots}$ and $S_{lmn \cdots}$ be any two tensors.
		Their tensor product is defined by $(T \otimes S)_{ijklmn\cdots} = T_{ijk\cdots} S_{lmn\cdots}$. Outer product is sometimes called \textbf{tensor product} or \textbf{Kronecker product}.
		\item \textbf{Contraction.} Let $T_{ijk\cdots}$ and $S_{ijk \cdots}$ be tensor of rank $m$ and $n$. Their contraction over the index $i$ is a tensor of rank $m+n - 2$ defined by $(T \cdot S)_{jk\cdots lm\cdots} = \sum_{i=0}^{n-1} T_{ijk\cdots} S_{ilm\cdots}$.
		If there are many different tensors with the corresponding contraction indices specified, their full contraction give a new tensor defined as 
		\begin{equation}
			\sum_{i_1=0}^{n_1-1} \sum_{i_2=0}^{n_2-1} \cdots \sum_{i_k=0}^{n_k-1} T^{(1)}_{i_1 j_1 k_1 \cdots} T^{(2)}_{i_2 j_2 k_2 \cdots} \cdots T^{(k)}_{i_k j_k k_k \cdots}
		\end{equation}\label{eq:contraction}
		where $i_1$ to $i_k$ are the indices to be contracted.
	\end{itemize}
\end{observation}

Addition and scalar multiplication of tensors are clear enough. 
Permutation of indices is generalisation of matrix transpose. 
For an example, consider ranked-two tensor $T$, and define $S_{ij} = T_{ji}$

\begin{equation*}
	T_{ij} =
	\begin{pmatrix}
		T_{00} & T_{01} & \cdots  \\
		T_{10} & T_{11} & \cdots \\ 
		\vdots & \vdots & \ddots \\
	\end{pmatrix}, \qquad
	S_{ij} =
	\begin{pmatrix}
		T_{00} & T_{10} & \cdots  \\
		T_{01} & T_{11} & \cdots \\ 
		\vdots & \vdots & \ddots \\
	\end{pmatrix}
\end{equation*}

Outer product and flattenning are demonstrated by figure \ref{fig:outer-flatten}.

\begin{figure}[ht]
\centering
\input{figures/outer-flatten.tikz}
\caption{(a) The outer product of a vector $v_i$ and a matrix $M_{jk}$ yields the rank-three tensor $T_{ijk}$. (b) Flattening the indices $i$ and $j$ of $T_{ijk}$ into a single index $\alpha = n_2\, i + j$ yields the matrix $S_{\alpha k}$.}
\label{fig:outer-flatten}
\end{figure}

Usually, outer product and flattenning are used together to convert tensor products of two arbitrary tensors into a single matrix, for example
\begin{equation}
	A \otimes \vec{v} = 
	\begin{pmatrix}
		a_{00} & a_{01}\\
		a_{10} & a_{11}
	\end{pmatrix}
	\begin{pmatrix}
		v_0 \\ v_1	
	\end{pmatrix}
	= 
	\begin{pmatrix}
		a_{00} v_0 & a_{01} v_0 \\
		a_{10} v_0 & a_{11} v_0 \\
		a_{00} v_1 & a_{01} v_1 \\
		a_{10} v_1 & a_{11} v_1
	\end{pmatrix}
\end{equation}

In general, if we have two matrices $M$ and $N$, their outer product will be 
\begin{equation}
	M \otimes N = 
	\begin{pmatrix}
		M_{00} N & M_{01} N & \cdots \\
		M_{10} N & M_{11} N & \cdots \\
		\vdots & \vdots & \ddots
	\end{pmatrix}
\end{equation}

Contraction is the more complicated operation. 
For the matrices, $M_{ij}$ and $N(jk)$, their contraction over the index $j$ is a matrix $P_{ik}$ defined by $P_{ik} = \sum_{j=0}^{n-1} M_{ij} N_{jk}$, which is exactly the matrix multiplication.

Given a set of tensors and the corresponding contraction indices. 
The contraction order does not matter, because we can freely change the order of and add parenthesis to nested summuations.

\begin{equation}
	\begin{split}
		\sum_{i_1=0}^{n_1-1} \sum_{i_2=0}^{n_2-1} \cdots \sum_{i_k=0}^{n_k-1} T^{(1)}_{i_1 j_1 k_1 \cdots} T^{(2)}_{i_2 j_2 k_2 \cdots} \cdots T^{(k)}_{i_k j_k k_k \cdots} 
		&=
		\sum_{i_2=0}^{n_2-1} \sum_{i_1=0}^{n_1-1} \cdots \sum_{i_k=0}^{n_k-1} T^{(2)}_{i_2 j_2 k_2 \cdots} T^{(1)}_{i_1 j_1 k_1 \cdots} \cdots T^{(k)}_{i_k j_k k_k \cdots} \\ 
		&= \sum_{i_1=0}^{n_1-1} \sum_{i_2=0}^{n_2-1} \cdots \sum_{i_k=0}^{n_k-1} T^{(1)}_{i_1 j_1 k_1 \cdots} \left( T^{(2)}_{i_2 j_2 k_2 \cdots} \cdots T^{(k)}_{i_k j_k k_k \cdots} \right) \\
		&= \sum_{i_1=0}^{n_1-1} \sum_{i_2=0}^{n_2-1} \cdots \sum_{i_k=0}^{n_k-1} \left( T^{(1)}_{i_1 j_1 k_1 \cdots} T^{(2)}_{i_2 j_2 k_2 \cdots} \right) \cdots T^{(k)}_{i_k j_k k_k \cdots} \\
		&= \cdots
	\end{split}
\end{equation}\label{eq:contraction-order}

This is an important observation and we will make it a lemma. 
\begin{lemma}[Contraction Order Does Not Matter]
	Given a set of tensors and the corresponding contraction indices, the contraction order does not matter.
\end{lemma} \label{lem:contraction-order}

\subsection{Abstract Tensor Product}

In multilinear algebra, the definition of tensor product is thus. 

\begin{definition}[Tensor Product of Vector Spaces]
	Let $V, W$ be vector spaces over a field $\F$. Their tensor product, denoted as $V \otimes W$, is the unique vector space over $\F$ that satisfies the following universal property as demonstrated in figure \ref{fig:tensor-product-universal}:
	there exists a bilinear map $\eta: V\times W \rightarrow V \otimes W $, and for all bilinear map $f : V \times W \rightarrow U$ to any vector space $U$, there exists a unique linear map $\hat{f} : V \otimes W \rightarrow U$ such that $f = \hat{f} \circ \eta$. 
\end{definition}

\begin{figure}[ht]
	\centering \input{./figures/tensor-product-universal.tikz}
	\caption{Commutative triangle for tensor product.}\label{fig:tensor-product-universal}
\end{figure}

Equivalently, the tensor product of two finite-dimensional vector spaces $V$ and $W$, with basis $v_1, \cdots, v_n$ and $w_1, \cdots, w_m$ is the $n\cdot m$ dimensional vector space $V \otimes W$ with basis $v_i \otimes w_j$ that satifies the following bilinearity conditions:
\begin{equation*}
	\begin{aligned}
		(v_i + v_j) \otimes w_k &= v_i \otimes w_k + v_j \otimes w_k, \\
		v_i \otimes (w_j + w_k) &= v_i \otimes w_j + v_i \otimes w_k, \\
		c (v_i \otimes w_j) &= (c v_i) \otimes w_j = v_i \otimes (c w_j), \quad c \in \F.
	\end{aligned}
\end{equation*}
where $i, j, k$ are the appropriate indices of the basis vectors.

I hope the original definitions of tensor product justifies our nomenclature of tensor in definition \ref{def:tensor}.
It is clear that 
\begin{enumerate}
	\item A rank-one tensor, $T_i$, is a vector in some vector space $V$,
		where the dimension of the index $i$ is the dimension of $V$.
	\item In general, a rank-$k$ tensor $T_{i_1 i_2 \cdots}$ have additions and scalar multiplications, so the set of all rank-$k$ tensor with fixed dimension for each of its index, denoted by $\T_{i_1, i_2 \cdots}$, is a vector space.
	This vector space has a set of basis $\{e_{j_1, j_2, \cdots, j_k}, \cdots\}$ , where $e_{j_1, j_2, \cdots,}(i_1, i_2, \cdots) = 1$ if and only if $i_1 = j_1, i_2 = j_2, \cdots$, and $0$ otherwise.
	It is clear that this basis is in one-to-one correspondence with the tensor product of vector spaces $V_0, V_1, \cdots, V_k$, where dimension of $V_i$ is the dimension of the index $i$.
	\item Flatten is the canonical isomorphism between the vector space $V_1 \otimes V_2 \otimes \cdots \otimes V_k \rightarrow (V_1 \otimes V_2) \otimes \cdots \otimes V_k$. (And unflatten its inverse.)
	\item Outer product $(\T_1, \T_2) \rightarrow \T_1 \otimes \T_2$ is a bilinear form, therefore this map is the canonical bilinear map guaranteed by the universal property of tensor product.
\end{enumerate}

In linear algebra, there is an intrinsic notion of input and output in linear algebra. 
For example, in vector space $V$ a vector is an element of $V$. Its dual is a function $V \rightarrow \C$, and a matrix is a function $V \rightarrow V$.
In physics, the input and output of tensor are differentiated by the upper and lower indices. 
For example, $\T^k_l$ denotes the class of tensor $ \underbrace{V \otimes V \otimes \cdots \otimes V}_{k} \otimes \underbrace{V^* \otimes V^* \otimes \cdots \otimes V^*}_{l} \cong \underbrace{V \otimes  \cdot \otimes V}_{l} \rightarrow \underbrace{V \otimes \cdots \otimes V}_{k}$,
where $V$ is a vector space and $V^*$ is its dual.
While differentiating input and ouputs seems an intuitive idea, it actually is unweldy in practice.
As our investigation of tensor product in this thesis is of an algorithmic nature, we will not differentiate input from output.

\section{Tensor Networks}

Suppose we have two tensors $T_{ijk}$ and $S_{kl}$, where index $i $ and $j$ have dimension $n_1$ and $n_2$ while both of index $k$ and $l$ have dimension $n_1$.
If we would like to perform the following operation: `contracting the first index of $T$ with the second index of $S$, and contracting the last index of the resulting tensor with the first index another copy of $T$.' 
The precise statement of specifying operations of tensor is too verbose and painful to articulate.
Tensor network, a graphical representation of tensors and their operations, is used to make these statement concise and clear.

\begin{definition}[Tensor Network]
	A tensor network is a generic graph structures consists of vertices and and edges that denotes the tensor resulted from the various operations on a set of tensors, following the following three rules.


\textbf{Rule 1}. In tensor network, a rank-$k$ tensor is represented as a node with $k$ edges.
Usually, the shape, color, and labels of the nodes are use to differentiate different tensors.
For example vector $v$, matrix $M$, and rank-three tensor $T$ are represented as in figure \ref{fig:simple_tensors}.

\begin{figure}[ht]
	\centering
	\input{./figures/tensor-net-nodes.tikz}
	\caption{Rank 1, 2, and 3 tensors}
	\label{fig:simple_tensors}
\end{figure}

\textbf{Rule 2}. A diagram with two tensors placed side by side is interpreted as the outer product of the two tensors, as shown in figure \ref{fig:tensor-product}.

\textbf{Rule 3}. An edge connecting two nodes represents the contraction of the two tensors over the index represented by that edge.
\begin{figure}[ht]
	\centering
	\input{./figures/tensor-product.tikz}
	\caption{Outer Product in Tensor Network}
	\label{fig:tensor-product}
\end{figure}

\end{definition}

We may also define tensor networks as a special kind of subgraph such that
\begin{enumerate}
		\item it allows loops and multiple edges,
		\item its vertices are of different types,
		\item the order of the edges of a vertex matters.
\end{enumerate}

The value of a tensor can be evaluated by plugging in the values of the indices. 
In tensor network, this is analogous to assigns a value to each of the edges and the graph shall be evalulated to some number. 

For example if we have tensor $H = \begin{pmatrix}
	1 & 2 \\
	3 & 4
\end{pmatrix} $. In explicit tensor notation, $H_{00} = 1, H_{01} = 2,  H_{10} = 3$ and $H_{11} = 4$.
In tensor network notation, $H$ is represented as a node with two edges. Labelling the edges with $0$ and $1$ respectively evaluates the tenor as shown in figure \ref{fig:tensor-net-eval}.

\begin{figure}[ht]
	\centering
	\input{./figures/tensor-eval.tikz}
	\caption{Evaluating a tensor network}
	\label{fig:tensor-net-eval}
\end{figure}
\subsection{Order of Edges}


While tensor networks do not differentiate input and output, it does different order of the bonds of a tensor. 
For example, figure \ref{fig:tensor-transpose} shows the transpose of $H$.
\begin{figure}[ht]
	\centering
	\input{./figures/tensor_transpose.tikz}
	\caption{Transpose in Tensor Network}
	\label{fig:tensor-transpose}
\end{figure}


The edges are typically ordered from top to bottom, from left to right. 
Orders can also be specified by labelling the edges with indices of the tensro. 
For example, in figure \ref{fig:contraction-matrix-mult} we demonstrated matrix multiplication, a non-commutative operation, in tensor network notation.

\begin{figure}[ht]
	\centering
	\input{./figures/contraction-matrix-mult.tikz}
\caption{Contracting the two rank-two tensors $M_{ij}$ and $N_{jk}$ over their shared index $j$ gives the rank-two tensor $(MN)_{ik} = \sum_j M_{ij} N_{jk}$}
	\label{fig:contraction-matrix-mult}
\end{figure}

\subsection{Only Topology Matters}

There is a subtlety in the graphical notation.
Each subgraph of the tensor network can be itself be regarded as new tensor result from the full contraction of its nodes.
For example in figure \ref{fig:contraction-matrix-mult}, the two nodes $M$ and $N$ can be regarded as a new node $MN$ with two edges, and in figure \ref{fig:tensor-product}, the two nodes $v$ and $w$ can be regarded as a new node $v \otimes w$ with two edges.
Given a tensor network, there are many different ways to decompose a tensor network into subgraphs and many different order for contractions, will all of them give the same result?

Luckily, lemma \ref{lem:contraction-order} guarantees that the answer is yes, as long as the nodes and their connections are determined.
The tensor network notation precisely captures the nodes and connection between them, so the tensor network notation is unambiguous and well-defined.
This property is summarised in the phrase `only topology matters'.

\subsection{Symmetric Tensors}

A special class of tensors is worth our attention. 

\begin{definition}[Symmetric Tensor]
	A tensor $T_{ijk\cdots}$ is called symmetric if it is invariant under any permutation of its indices, i.e. for any permutation $\sigma \in S_k$, we have
	$$
		T_{i_1 i_2 \cdots i_k} = T_{i_{\sigma(1)} i_{\sigma(2)} \cdots i_{\sigma(k)}}.
	$$
\end{definition}\label{def:symmetric-tensor}

In tensor network notation, permuting the edges of a symmetric tensor does not change the tensor, as shown in figure \ref{fig:symmetric-tensor}.

\begin{figure}[ht]
	\centering
	\input{./figures/symmetric-permutation.tikz}
	\caption{Permuting the edges of a symmetric tensor does not change the tensor.}
	\label{fig:symmetric-tensor}
\end{figure}

Tensor networks that consists of only symmetric tensors are be regarded as colored hypergraphs, as the order of the edges of a symmetric tensor does not matter.
Therefore, if there are no dangling legs, the networks of symmetric tensors can indeed be regarded as a coloured hypergraph, and two networks that are isomorphic as hypergraphs are equivalent as tensor networks.

If there are dangling legs, the order and connection to those legs still matters, as contraction between symmetric tensors may give non-symmetric tensors.
In this case, we can regard each dangling leg as a normal edge connection to a special nodes colored in its unique color.

We shall summarised this obeservation as \define{only connectivity matters}.

\begin{lemma}[Only Connectivity Matters]
	Two tensor networks without dangling legs that are composed of only symmetric tensors are equivalent if they are isomorphic as coloured hypergraphs.
	That is, they equal to the same tensor if they have the same nodes and connections between them.

	If they do have dangling legs, the ordering of the dangling legs still matters. 
\end{lemma}\label{lem:only-connectivity-matters}

\section{Complexity of Contraction}

Let us investigate the algorithmic complexity of tensor conctraction, which is a a problem of paramount importance as it is used in so many applications, such as quantum circuit simulation, machine learning, and almost all kinds of mathematics modelling.

For simplicity, we assume all index in this section have dimension $d$.

Contraction of two rank-2 tensors, $M_{ij}$ and $N_{jk}$, gives another rank-2 tensor $P_{ik}$.
Each entry of $P$ is computed using the formula
$$
	P_{ik} = \sum_{j=0}^{d-1} M_{ij} N_{jk}	
$$
Computing a single entry thus takes $d$ multiplications and $d-1$ additions.
There are a total of $d^2$ entries to compute, so the total number of operations is $d^2(d + d - 1) = O(d^3)$.

Similarly, contraction of on bond between a rank $m$ and a rank $n$ tensor will result in a rank $m + n - 2$ tensor. 
Each entry of ther resulting tensor is computed by summing over $d$ products of entries from the two tensors, so the total number of operations is $O(d^{m+n-1})$.

We may also choose to contract multiple bonds simultaneously. 
In general, contracting $k$ bonds between a rank $m$ and a rank $n$ tensor will result in a rank $m + n - 2k$ tensor.
Each entry of the resulting tensor is computed by summing over $d^k$ products of entries from the two tensors, so the total number of operations is $O(d^{m+n-k})$.

It is thus clear that contracting $k$ bonds simultaneously over a tensor network with $n$ external bonds will take $O(d^{n+k})$ operations.
This is an important observation that worth to be written down.

\begin{lemma}[Complexities of Tensor Contraction by Counting Edges]\label{lem:contraction-complexity}
	Let $T$ be a tensor network with $n$ external bonds and $k$ internal bonds. 
	Contracting all the internal bonds of $T$ will take $O(d^{n+k})$ operations.
\end{lemma}

It may surprise the reader that different orders of contraction may have different computation costs.
For example, contracting the following tensor fully, which will gives a scalar, can be done in two different orders as shown in figure \ref{fig:contract-order-slow} and \ref{fig:contract-order-fast}.
The first contraction order takes $O(d^5)$, while the second contraction order takes $O(d^4)$.

\begin{figure}[ht]
	\centering
	\input{./figures/contract-order-slow.tikz}
	\caption{Slow contraction order}
	\label{fig:contract-order-slow}
\end{figure}
\begin{figure}[ht]
	\centering
	\input{./figures/contract-order-fast.tikz}
	\caption{Fast contraction order}
	\label{fig:contract-order-fast}
\end{figure}

\subsection{Source of Difference in Complexity}

As equation \eqref{eq:contraction-order} shows, differet orders of tensor contraction corresponds to different order of the nested summation, which is a result of three basic rules of arithmetic: commutativity, associativity, and distributivity.
It may puzzle the readers that why simple arithmetic identities can lead to such a difference in computation costs.
Let me break it down in this section.

Consider the following identity
$$
	(a_1 + a_2 + \cdots + a_n)(b_1 + b_2 + \cdots + b_m) = a_1 b_1 + a_1 b_2 + \cdots + a_n b_m
$$

The left-hand side of the equation takes $n-1$ additions for $a_i$, $m-1$ additions for $b_i$, and one multiplication between the sums, which is a total of $m+n -1$ operations.
The right hand side, however, takes $nm$ multiplications between $a_i$ and $b_i$ and $nm-1$ additions, amounting to a total of $2nm - 1$ operations. 
This is a big difference all caused by the distributive law.

The impact of order of operation for matrices is even more pronounced.
For example, consider $\vec{v_1}, \vec{v_2} \in \C^n $ and $M \in \C^{n \times n}$. 
Consider the following operations calculated in two different orders:
$$
(M\vec{v_1}^T ) \vec{v_2} = M(\vec{v_1}^T \vec{v_2})
$$

The left-hand side take two matrix-vector multiplications, which is $O(n^2)$ operations, while the right-hand side takes one outer product, costing $O(n^2)$ and one matrix-scalar multiplication, consting $O(n^3)$ operations.

% TODO: Is this statement true?
In general, both finding the optimal order and determing if a given order is optimal are NP-hard problems.
There are, nevertheless, many heuristic algorithms to find a good contraction order.
In this thesis, we will discuss in detail one of such algorithm which relies on a graph theoretical property called treewdith.


